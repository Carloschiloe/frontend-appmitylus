# Frontend Mitynex
#
# La app usa rutas relativas `/api` y el proxy/rewrite del proveedor de hosting.
# No declarar secretos en variables VITE_. Todo secreto pertenece al backend.
#
# Ejemplo si en el futuro se necesita una bandera publica:
# VITE_PUBLIC_ENV=staging
